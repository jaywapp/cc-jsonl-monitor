# 파일 트리 뷰어 화면 기준

## 방향

Operate 모드. 사용자가 지정한 2분할 레이아웃을 기준으로 한다. 좌측 파일 탐색기는 실제 폴더 계층, 우측은 선택한 항목의 작업 공간이다. 제안서의 밝은 배경과 절제한 녹색 강조를 이어 쓰되 문서용 큰 제목·소개 영역은 실제 탐색 화면 앞에 두지 않는다.

## 구성

- 상단: 앱 이름과 시스템/밝게/어둡게 테마. 서버 모드는 기준 경로 입력, 확장은 폴더 연결·파일 열기·최근 연결·폴더 내 상대 경로 이동.
- 좌측: 루트 경로, 폴더 펼침·접힘, JSONL 선택, 목록 새로고침, 읽기 전용 상태.
- 우측 폴더: 경로 탐색, 하위 폴더·파일 목록.
- 우측 파일: 파일·세션·cwd 정보, 검색·필터, 시간순 기록, 상세 항목 펼침, 변경 알림과 페이지 이동.
- 화면이 좁으면 탐색기를 상단에 고정 높이로 배치하고 내용을 아래에 둔다. 탐색기 접기 버튼도 제공한다.

## 표현

밝은 본문 배경, 옅은 회색 탐색기/도구 모음, 기본 텍스트 `#26343d`, 강조 `#235d4b`, 선택 배경 `#e7f0eb`, 오류 `#a32d35`. 한글 시스템 글꼴과 경로용 고정폭 글꼴을 사용한다. 아이콘은 Lucide로 통일한다. 공식 외부 디자인 시스템을 복제한 화면이 아니다.

DESIGN_VARIANCE 3, MOTION_INTENSITY 1, VISUAL_DENSITY 7. 이벤트는 불필요한 카드 중첩 없이 행 단위로 읽는다. 색만으로 구분하지 않고 요청·응답·도구·오류 라벨을 함께 표시한다. 기록은 설명과 항목 목록으로 렌더링하며 이미지를 자동으로 불러오지 않는다.

## 상호작용과 검증

파일 선택 시 요청 취소를 통해 이전 파일 응답이 뒤늦게 표시되지 않게 한다. 긴 대화는 접기와 페이지 이동으로 다룬다. 변경 감지는 안내만 하고 읽던 내용을 자동 교체하지 않는다. 경로/서버/파싱 오류, 빈 폴더·파일·검색 결과, 읽기 중 상태를 구분한다. 키보드 포커스와 방향키 탐색을 제공한다.

브라우저 검증은 데스크톱, 390/320px, 200% 확대를 포함한다. 스크린리더 전체 검증과 제품 성능 벤치마크는 별도 과제다.

## 확장과 다크모드

기존 2분할 구조와 녹색 강조를 유지한다. 모든 표면·텍스트·테두리·상태색은 CSS 변수로 관리한다. 다크모드는 배경 #11171d, 본문 #182027, 텍스트 #e0e7ec, 보조 텍스트 #a3b1bc, 강조 #95d2b4를 사용한다. 시스템 설정을 기본으로 따르고 수동 선택을 저장한다.

기록 제목은 복수 선택 버튼과 전체/선택 해제를 제공하며, 오류 상태 필터와 분리한다. 선택 여부는 aria-pressed로 표현한다. 최근 연결의 삭제는 원본이 아닌 연결 목록에서 제거하는 동작임을 레이블로 안내한다.

이벤트는 제목 → 설명 → 상세 항목 순서로 읽는다. 상세 항목은 기본 4개를 보여주고 나머지는 펼치며, JSON 원문 버튼과 코드 덩어리 대신 이름·값 목록을 사용한다. 기타 형식은 해석 범위를 안내하고 원래 행 번호로 출처를 표시한다.
