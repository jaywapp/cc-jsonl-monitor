# 제품 맥락

## 확인된 요구사항

Claude Code 사용 중 생성되는 프로젝트별 JSONL을 읽어 워크스페이스와 세션별 작업 흐름을 시간순으로 파악한다. 사용자는 파일 경로를 직접 지정할 수 있어야 한다.

2026-09-15 구현 지시로 레이아웃을 확정했다: **좌측에는 기준 경로의 파일 트리, 우측에는 선택한 항목의 내용**. 폴더 선택 시 하위 목록, JSONL 선택 시 해당 기록을 표시한다.

## 플랫폼과 구현

Chrome Manifest V3 확장과 로컬 Node.js 서버 두 실행 방식을 제공한다. React/TypeScript 화면과 파서·검색 로직을 공유한다. 확장은 사용자가 폴더 또는 파일을 선택해 읽기 권한을 부여하며 서버 없이 동작한다. 서버 버전은 절대 경로 직접 입력을 유지한다. 개발 환경은 Node.js 22.12 이상, 확장은 Chrome 데스크톱 122 이상을 대상으로 한다.

## 범위

파일 탐색, 경로 입력, 시간순 기록, 필터·검색, 상세 항목, 변경 확인을 제공한다. 2026-09-15 추가 지시로 확장 폴더 연결·파일 선택창·최근 연결 복원, 시스템/밝게/어둡게 테마, 기록 제목별 복수 선택 필터를 구현한다. 전체 프로젝트 검색, 영구 검색 인덱스, 클라우드 공유, 실행 제어와 탭 종료 후 상시 감시는 포함하지 않는다. 구체적 한도는 README에 기록한다.

## 데이터 원칙

원본 읽기 전용, 선택한 경로만 허용, 외부 로그 전송 없음. 화면의 사용자 역할을 실제 사람의 ID로 해석하지 않는다. 메타데이터가 없는 경로/시각/세션과 확인되지 않은 호출 관계를 만들어 내지 않는다.

2026-09-15 기록 해석 개편: 진행·요약·파일 이력·세션 정보를 분류하고, 기타 형식의 중첩 값도 읽을 수 있는 항목으로 투영한다. 화면에 JSON 원문을 제공하지 않으며 생략·분류 미확정 상태를 알린다.

2026-09-15 추가 요구: 파일·폴더·연결 전환 중 필터를 유지한다. 필터는 뷰어 탭의 메모리에만 보관하고 새로고침 시 초기화한다. 대화형 말풍선과 작업 내역 접기를 기본으로 제공하며 목록형을 선택할 수 있다. 메시지 전송 기능은 포함하지 않는다.

2026-09-15 우선순위 조정: 모바일 최적화보다 데스크톱에서 세션 대화 흐름을 넓게 읽는 것이 최대 목적이다. 상단 연결·설정의 세로 적층과 큰 파일 정보 영역을 줄인다.

2026-09-16 작업 패턴 분석: 선택 파일 전체를 외부 전송 없이 분석하여 요청 이후 흐름, 도구 사용, 반복 표시 입력, 오류 뒤 같은 호출 후보를 근거 기록과 함께 제공한다. 의도·효율·완료 여부를 단정하지 않는다. 기존 분석 한도를 그대로 적용하고 세션·입력 미확정 상태를 명시한다.
